name = "wedlune-web"
pages_build_output_dir = "./dist"
compatibility_date = "2026-08-07"

# Preserve variables and secrets managed in the Cloudflare dashboard when
# Wrangler publishes a new build. Without this, deploys can replace the remote
# variable set with only the values declared in this file.
keep_vars = true
